# GitHub App Configuration
# Get these values from https://github.com/settings/apps after creating your GitHub App

# The ID of your GitHub App
APP_ID=your_app_id_here

# The webhook secret you set when creating the app
# IMPORTANT: This MUST match exactly with the webhook secret configured in your GitHub App settings
# To verify/update:
#   1. Go to https://github.com/settings/apps
#   2. Click on your app name
#   3. Scroll to "Webhook secret (optional)" section
#   4. Ensure the secret here matches what you set in GitHub
# If mismatched, the app will reject webhook payloads with signature verification errors
WEBHOOK_SECRET=your_webhook_secret_here

# The private key for your GitHub App (path to .pem file or the key contents)
# You can either:
# 1. Set this to the path of your private key file:
PRIVATE_KEY_PATH=path/to/private-key.pem
# 2. Or set the private key contents directly (replace newlines with \n):
# PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----"

# Optional: GitHub Enterprise Server URL (omit for github.com)
# GHE_HOST=github.example.com

# Optional: Webhook proxy URL for local development (e.g., Smee.io)
# WEBHOOK_PROXY_URL=https://smee.io/your-unique-channel

# Optional: Port for the Probot server (default: 3000)
# PORT=3000

# Optional: Log level (trace, debug, info, warn, error, fatal)
# LOG_LEVEL=info

# Optional: Node environment
# NODE_ENV=production
